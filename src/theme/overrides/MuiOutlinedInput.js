import palette from '../palette';

export default {
  input: {
    color: palette.brandGray,
    "&::placeholder": {
      color: palette.brandGray,
      opacity: 1
    },
    // color: "white",
  }
};
